import { StyleSheet } from "react-native";

const globalStyles = StyleSheet.create({
  background: {
    backgroundColor: "#000",
  },
  text: {
    color: "#fff",
  },
});

export default globalStyles;
